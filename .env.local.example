# Copy to .env.local and change. This is the password to open the calculator.
# NOTE: NEXT_PUBLIC_ vars are visible in the browser bundle. This is a light
# access gate for a private tool, not hardened authentication. See README.
NEXT_PUBLIC_APP_PASSWORD=ammex
